#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int n, ai[1000], i, sum = 0;
    scanf("%d", &n);
    for(i = 0;i < n; i++){
        scanf("%d",&ai[i]);
        sum = sum + ai[i];
    }
    printf("%d", sum);
    
    return 0;
}
